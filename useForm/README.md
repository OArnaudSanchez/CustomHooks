# useForm React Custom Hook

Usage Examples:
```
    const initialState = {
        input1: '',
        input2: '',
        input3: '',
        ...
    };
    const [ formValues, handleInputChange, reset ] = useForm(initialState);
```