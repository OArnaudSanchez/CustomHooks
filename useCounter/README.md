# useCounter React Custom Hook

Usage Examples:
```
    const initialCounter = {
        initialCounterValue: 0 //Counter value
    }
    const { counter, incrementCounter, decrementCounter, resetCounter } = useCounter(initialCounter);
```